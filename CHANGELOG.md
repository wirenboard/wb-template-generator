# Changelog

Все заметные изменения проекта документируются в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/),
проект придерживается [Semantic Versioning](https://semver.org/lang/ru/).

## [0.8.0] - 2026-09-04

### Добавлено
- **Автоматический выкат в прод при мерже в `main`** и **откат одной кнопкой** (Jenkins → rollback). Образы собираются в CI и метятся `<ветка>-<git-SHA>`, сервер скачивает готовый образ — прошлая версия возвращается за секунды. Как пользоваться — `DEPLOYING.md`.
- **smoke-проверка после выката**: если сервис не ответил или отдаёт не тот коммит, выкат считается неудачным и откатывается автоматически.
- **Журнал выкатов** (Environments → production): видно, что и когда попало в прод.
- **`revision` в `/api/status`** — git-SHA работающего образа.
- **`make`** как единый вход для проверок: `make lint`, `make test`.

### Изменено
- **Порт прода публикуется только на `127.0.0.1`** — `${WEB_PUBLISH:-127.0.0.1:8080}:80`. Прежняя запись `${WEB_PORT:-80}:80` открыла бы контейнер на всех интерфейсах, то есть наружу в обход TLS и мимо firewall (правила публикации docker ставит сам). Снаружи сервис по-прежнему отдаёт nginx на хосте. Адрес задаётся одной переменной, а не парой, и назван иначе, чтобы не столкнуться с `WEB_PORT` из существующего `.env`.
- Backend-образ запускается сам по себе: `CMD` и `HEALTHCHECK` смотрят в `:9000` (раньше — в `:8000`, порт задавался только в compose).
- Frontend отдаёт `/healthz` и имеет HEALTHCHECK, nginx перечитывает адрес backend — при перезапуске бэкенда больше нет 502 на `/api`.

### Удалено
- **`docker-compose.prod.yml`** — он собирал образ на сервере, такой выкат нечем откатить. Прод-конфигурация одна и живёт в `wirenboard/infra` (роль `wb_template_generator`).
- Файл выката переехал в `wirenboard/infra` (роль `wb_template_generator`) — у него один дом, копий в репозитории сервиса нет.
- Конвейер переехал в Jenkins: `Jenkinsfile` (проверки) / `Jenkinsfile.deploy` / `Jenkinsfile.rollback`. В `ci.yml` остались только проверка CHANGELOG и выпуск тега — того, чего в Jenkins нет.

## [0.7.10] - 2026-09-04

### Изменено
- **Тулбар таблицы закреплён сверху.** Кнопки «Добавить», «Удалить» и выпадающие меню нужны там, где идёт правка, а раньше при длинном списке за ними приходилось прокручивать страницу в начало. Запрошено с форума.

## [0.7.9] - 2026-09-03

### Добавлено
- **Горячие клавиши для строк таблицы: `Insert` добавляет регистр, `Delete` удаляет.** Insert открывает «Адрес» новой строки и работает прямо во время ввода, поэтому вместе с переходом по Tab ввод идёт без мыши; кнопка «+ Добавить» теперь тоже открывает «Адрес». Delete удаляет отмеченные чекбоксами строки, иначе ту, где стоит фокус; вернуть можно тостом отмены или Ctrl+Z.

### Изменено
- **Проверка «пользователь набирает текст» стала одной на весь фронтенд** (`isTypingTarget`) и перестала считать полем ввода чекбоксы, радио и кнопочные `input` — раньше каждое место проверяло только имя тега. От этого `Ctrl+Z` и вставка картинки по `Ctrl+V` больше не глохнут, когда фокус стоит на таком элементе, например на скрытом поле выбора файла после диалога.
- **Счётчик на кнопке «Удалить» считает только существующие строки.** Отметки переживали удаление строки кнопкой «x», сброс шаблона, импорт и AI-анализ, поэтому кнопка показывала строки, которых уже нет, и ничего не удаляла.

## [0.7.8] - 2026-09-02

### Добавлено
- **Переход между ячейками таблицы по Tab прямо во время ввода.** Tab коммитит значение и открывает соседнюю колонку, Shift+Tab — предыдущую: ввёл адрес, Tab, печатаешь имя. С «Группы» Tab выводит на «R/O», дальше строка обходится штатным порядком. Невыбранная ячейка открывается по Enter или F2. Запрошено с форума.

### Изменено
- **Списки в таблице («Тип», «Формат», «Ед.», «Канал», «Группа») закрывают ячейку при уходе фокуса, а не сразу при выборе значения.** Значение применяется по-прежнему сразу, но ячейка остаётся открытой — иначе смена значения стрелкой закрывала её мгновенно и обход по Tab обрывался на первом же списке.

### Исправлено
- **Tab больше не проваливается мимо колонок в чекбокс «R/O».** Невыбранная ячейка рисуется как `<span>`, а без `tabIndex` он выпадает из табуляции: фокус в строке получали только чекбоксы и кнопки, а «Адрес» и «Имя» — то, что и заполняют руками — обходились стороной. Внутри открытого поля Tab терялся по другой причине: `onBlur` убирал поле из DOM.

## [0.7.7] - 2026-08-31

### Добавлено
- **Тесты**: `serial-values.test.ts`, `number-input.test.ts` (фронтенд), `test_serial_values.py` (бэкенд), контракт близнецов `serial_values_contract.json` — общие кейсы разбора и порядка сортировки для обоих сьютов.

### Исправлено
- **Hex-адрес регистра сохраняется, а не обнуляется.** Ввод разбирал `parseInt(value, 10)`, а он останавливается на «x», и «0xff» превращалось в 0 - при том что драйвер такую запись принимает, а даташиты печатают адреса именно в hex. Записи теперь разделяются по форме: цифры дают число, `0xFF` остаётся строкой с префиксом в нижнем регистре (схема требует именно `0x`), побитовая запись и неразобранное уходят в состояние как введены и достаются валидатору. Тем же разбором пользуется импорт CSV. Подсказка и заполнитель у столбца «Адр.» упоминают hex, сортировка регистров после анализа больше не считает hex-адрес нулём.
- **Дробное значение набиралось не тем, что вводили, а значение поля не стиралось.** Числовые поля - `scale`, `offset`, `round_to`, `min`, `max`, `default_value`, `on`/`off`, размер строки, значение enum, настройки LLM - были размечены как `type="number"`, а такое поле на незавершённом вводе отдаёт пустой `value`. В паре с `parseFloat(value) || 1` у `scale` это возвращало единицу: набор «0.5» после backspace давал 15 (пусто → 1, «0» → 10, «.» → 1, «5» → 15), и значение меньше единицы задавалось только обходным путём. Поля стали текстовыми с `inputMode`, незавершённый ввод не меняет состояние, пустое поле означает отсутствие значения, точка и запятая равноправны, при уходе из поля текст приводится к виду состояния. Стрелки для шага значения у таких полей пропадают.
- **Экспорт в `.json.jinja` больше не падает на hex и побитовых адресах.** Детекция строковых паттернов вычитала адреса каналов друг из друга не глядя на тип, и три однотипных канала с адресами вида `0x1D0` роняли её в `TypeError` - пользователь получал 500 без объяснения. Прогрессия считается только там, где адрес записан числом: цикл выражает адрес как «база + шаг», и hex стал бы в нём десятичным числом. Каналы с hex и побитовыми адресами разворачиваются в список, сохраняя запись. Побитовые адреса роняли экспорт и раньше - они приезжают из импорта готового шаблона.
- **Импорт шаблона сохраняет запись адреса.** `int(addr, 0)` разворачивал `0x012F0000` в 19857408, и такой шаблон получал ошибку «недопустимый адрес» на каждом канале: развёрнутый четырёхбайтный код выходит за 16-битный диапазон, из проверки которого hex-запись исключена намеренно. Импорт пользуется тем же разбором, что и поле ввода.
- **Регистр с hex-адресом не теряется из-за ошибки в соседнем поле.** Когда регистр не собирается целиком, разбор падает на мягкую ветку и берёт из него только адрес с именем. Ветка делала `int(address)`, поэтому регистр с hex-адресом отбрасывался заодно с ошибкой, из-за которой она сработала. Обе такие ветки — при разборе ответа модели и в автофиксе — пользуются общим разбором адреса.
- **Сортировка таблицы по адресу считает значение, а не строку.** Пара «число и строка» уходила в сравнение строк, и `0x10` вставало перед 2, а `0xff` перед 9. Столбец «Адр.» сортируется по значению, а записи без числа (OBIS-коды вроде `1.0.1.8.0.255`) сравниваются между собой строкой.
- **Дубликат адреса находится независимо от формы записи.** Ключ строился из адреса как есть, поэтому 255 и `0xff` считались разными регистрами - как и `0xff` против `0xFF`. Ключ приводится к канонической записи, тем же порядком дедуплицируются результаты анализа.
- **Импорт приводит дробные значения к числу.** `scale`, `offset`, `round_to`, `min` и `max` шаблон приносит и строкой - с точкой или с запятой. Импортёр копировал её как есть, шаблон открывался в редакторе, но не собирался обратно: строку с запятой отвергала модель, а пользователь получал «Ошибка сборки: 422» без указания поля.
- **Поля, где схема разрешает две записи, принимаются в обеих.** `error_value`, `on_value` и `off_value` (`definitions/serial_int`) бывают записаны и числом, и hex-строкой, а модель редактора принимала одну запись на поле и отвергала регистр целиком. Часть шаблонов wb-mqtt-serial из-за этого импортировалась не целиком, у отдельных приборов терялось больше половины каналов. Запись сохраняется как пришла, нераспознанное помечается ошибкой `validation.invalidNumber`. Поля `On` и `Off` в панели деталей hex-запись и принимают — у somfy это код команды вида `0x0A0404`, считать его десятичным эквивалентом руками незачем.
- **Адрес из документа доезжает в той записи, в какой напечатан.** Правила извлечения велели модели переводить hex в десятичное, а формат ответа тем временем hex разрешал. Осталось одно правило — запись из документа сохраняется, драйвер hex принимает. Кейс с hex-адресами добавлен в ручной набор `tests/prompt_regression`, там же сверка адреса теперь требует, чтобы запись осталась hex, а не просто совпала по значению.
- **Модель просят писать дробные числа точкой.** Примеры в промпте были с точкой, но правила не было, а даташиты в русской локали печатают запятую. Значение с запятой модель редактора отвергала, и регистр уходил в мягкую ветку с дефолтами - молча терял `scale`, `format` и `reg_type`.
- **Hex в лимитах разворачивается в число при импорте.** Схема разрешает hex-запись в `min` и `max` канала (`serial_num`), но у параметра требует plain number, а драйвер лимиты параметра копирует в JSON Schema формы настроек, где границей бывает только число. Поэтому редактор держит лимиты числом, разворачивая запись при импорте шаблона и при разборе ответа модели. Шаблон с `"max": "0xff"` у параметра раньше собирался и падал на проверке схемы — `'0xff' is not of type 'number'`. Неразобранная запись поле не занимает, регистр приезжает без неё.
- **Запись числового поля проверяется паттерном схемы.** `serial_int` разрешает целое и hex, `serial_num` добавляет дробную часть, экспоненту не разрешает ни один. Прежняя проверка пропускала `"error_value": "1e5"` — такой шаблон открывался в редакторе и падал на выгрузке. Запись с пробелами по краям тоже помечается, при импорте пробелы обрезаются. Паттерны читаются из вендоренной схемы там же, где перечни `reg_type` и `format`.
- **`scale` равный нулю помечается ошибкой.** Канал с ним читает константный ноль, а отключают каналы флагом `enabled`, поэтому законного применения у нуля нет, в шаблонах драйвера он не встречается. Прежнее `parseFloat(value) || 1` глотало ноль заодно с пустым полем, после перехода на текстовое поле он стал доезжать до шаблона молча.
- **Ошибки новых проверок приходят в LLM-петлю текстом, а не ключом локализации.** Ключ вроде `validation.invalidNumber` модель исправить не может — в ретрай-промпт уходит фраза с полем, значением и допустимой записью (`on_value "1.5" is invalid. Use a number, or a hex string like "0xFF"`), а про нулевой `scale` сказано, что канал всегда читает ноль. Подсказка про адрес в той же петле упоминает hex-запись.
- **Гигантская запись значения не роняет импорт.** `int` из hex-записи не ограничен лимитом CPython, и `"0x" + "F"×3600` в лимите разворачивался в число, которое не сериализуется в JSON — ответ `/api/import-template` падал в 500 уже вне обработчика. Запись шире u64 считается неразобранной (драйвер читает значения через `stoull`), а гигантская десятичная запись адреса остаётся строкой.
- **Целая запись длиннее 15 цифр не искажается.** Разбор шёл через float, и `"18446744073709551615"` превращалось в `…16`. Целые парсятся напрямую.
- **Экспонента больше не даёт `inf`.** `float("1e400")` возвращал бесконечность, она уезжала в ответ импорта, а Starlette сериализует с `allow_nan=False` — пользователь получал 500 вместо разбираемого отказа.
- **Сортировка по адресу упорядочивает биты одного регистра.** Ключ считался по части до первого двоеточия, поэтому `109:0:1` и `109:0:2` давали ничью. Столбец «Адр.» и порядок результатов анализа сравнивают адреса по всем частям записи. Общий порядок python- и ts-разбора закреплён фикстурой `serial_values_contract.json`, идентичность её копий сторожит тест бэкенда на полном чекауте.
- **Настройкам LLM вернулись границы.** При переходе на текстовое поле потерялись `min`/`max`/`step`, стоявшие атрибутами у `type="number"`: число токенов и таймаут снизу, температура в диапазоне 0..2. Температура теперь ограничена и на сервере — `llm_temperature` принимает 0..2 на всех маршрутах, включая `/api/analyze`, а сохранённое в браузере значение зажимается в диапазон при загрузке.

## [0.7.6] - 2026-08-27

### Добавлено
- **Потолки на разбор присланных файлов.** Число файлов в запросе ограничено настройкой `MAX_FILES` (10), xlsx не должен разворачиваться больше чем в 5 МБ и давать больше 500 тыс. символов текста, изображение — быть больше 25 Мпикс. Объём архива считается по оглавлению, до открытия книги, поэтому отказ мгновенный, а объём текста — до сборки строки листа. Крупные изображения уменьшаются сразу при открытии, а не перед отправкой в модель.
- **Потолок размера у импорта шаблона.** `/api/import-template` читал файл целиком в память без ограничения — внутри импортёра потолок стоял только на Jinja-исходнике, а JSON-ветка и само чтение шли без него. Теперь действует тот же `MAX_REQUEST_SIZE_MB`, что и у анализа.
- **Потолки на поля запроса** (`backend/models.py`): длина строк 512 или 2048 символов по типу поля, до 5000 каналов, 2000 групп, 4096 значений enum и 10 тыс. строк в запросе на перевод. Детекция паттернов в jinja-экспорте копирует строку на каждое число в ней, поэтому имя канала из одних цифр давало квадратичный расход. Дополнительно ограничено число рассматриваемых чисел в одной строке.
- **Счётчики обращений по маршрутам в `/api/metrics`** — `endpoints` и `endpoint_errors` показывают, какими действиями пользуются в интерфейсе и где чаще отказы. Ключ берётся только из списка зарегистрированных маршрутов, поэтому сканер не накачает словарь несуществующими путями.
- **Тесты**: `test_error_disclosure.py`, `test_log_utils.py`, `test_upload_limits.py`, `test_analyze_file_rejection.py`, `test_field_limits.py`, `test_custom_prompt.py`, `test_cors.py`, `test_endpoint_metrics.py`.

### Изменено
- **Текст провайдера LLM больше не уходит наружу.** Отказы обращения к LLM описываются категорией сбоя — ключ, квота, лимит частоты, таймаут, отказ соединения, ошибка провайдера, — а не сырым сообщением исключения. Адрес LLM присылает клиент, и различия в тексте (отказ соединения, ошибка DNS, TLS, HTTP-статус) отвечали на вопрос «что стоит по этому адресу», то есть работали оракулом по чужой сети. Категория приходит ключом локализации и переводится в интерфейсе, подробности остаются в логе сервера — на анализе и исправлении регистров текст провайдера в лог не писал никто, и отказ было нечем диагностировать. Затронуты `/api/models`, `/api/translate`, анализ и исправление регистров.
- **Классификатор ошибок разбирает сбои httpx.** Список моделей запрашивается напрямую через httpx, минуя openai SDK, поэтому его таймаут и отказ соединения попадали в категорию «неизвестно». Побочно это лечит пустую причину в 502 у `/api/models`: у `httpx.ReadTimeout` пустой текст, и в ответе оставалось «Не удалось получить список моделей — .».
- **Битый файл пользователя больше не поднимает дежурных.** Категория `bad_request` понижена с критической до предупреждения: 400 от провайдера означает, что модель не приняла присланный файл, а критическая серьёзность шлёт алерт в чат немедленно, то есть на каждый кривой PDF. `not_found` осталась критической — 404 на серверном ключе означает, что заданной модели нет, то есть анализ не работает ни у кого.
- **Пользовательский промпт рендерится заменой, а не `str.format`.** Шаблон приходит от пользователя, а format давал ему спецификаторы формата, доступ к атрибутам и падение на опечатке: `{template_type:>2000000000}` клал процесс по памяти, то есть один запрос ронял сервис для всех. Результат подстановки ограничен 100 тыс. символов, размер предсказывается арифметикой до аллокации. Тот же потолок стоит и на дефолтном промпте — список языков приходит формой и на серверном ключе уезжает в промпт как есть. Незнакомый плейсхолдер теперь остаётся текстом.
- **Кросс-доменные запросы по умолчанию запрещены.** Дефолт `CORS_ORIGINS` сменился с `*` на пустую строку, а `allow_credentials` выключен: с прежней парой middleware отражал origin запросившего и разрешал креденшелы, то есть любая страница могла дёргать API из браузера пользователя и читать ответы. Интерфейсу CORS не нужен — он ходит на относительные пути через тот же origin. Из `docker-compose.prod.yml` переменная убрана совсем: в записи `${CORS_ORIGINS:-http://localhost}` пустое значение неотличимо от незаданного, поэтому запретить кросс-доменные запросы через прод-`.env` было нельзя — на месте пустой строки подставлялся дефолт.
- **Перевод переведён на общий код LLM.** Маршрут звал провайдера сам, своей копией цикла правки параметров и своим разбором ответа. Теперь он зовёт те же `call_llm` и `extract_json_from_response`, что и анализ, а отказ обращения к LLM пишется в лог одним форматом на всех четырёх маршрутах.
- **Токен Telegram-бота вырезается из лога.** Логгер httpx на уровне INFO печатает полный URL, поэтому каждый алерт оставлял в логе действующий токен открытым текстом. Вырезает фильтр логирования, а не поднятый уровень логгера — иначе из лога ушли бы и строки обращений к LLM.
- **Uvicorn больше не пишет свой access-log** (`--no-access-log` в обоих compose и в `CMD`). Он дублировал записи нашего middleware и печатал строку запроса целиком, то есть ручной вызов API с ключом в query снова положил бы секрет в лог.
- **Управляющие символы из присланных значений экранируются в логе** (`backend/log_utils.py`) — путь запроса, имя хоста из адреса LLM, имена файлов, имя листа Excel, имена и сырые тела регистров из ответа модели, текст отказа импорта. ESC в терминале исполняется как команда, то есть присланной строкой можно было покрасить или спрятать соседние записи от читающего лог. Ответ модели сюда входит потому, что со своим адресом LLM его содержимое задаёт сам клиент — по той же причине значение в логе обрезается по 1000 символов - текст сбоя провайдера приезжает целой HTML-страницей.
- **Отказ в тулбаре виден целиком** — раньше текст обрезался по 192 px и полностью читался только по наведению мыши, недоступному на тач-устройствах. Обрезка убрана вместе с причиной её появления: дампа провайдера в этом слоте больше нет, а короткая фраза по категории переносится по словам, а не обрезается.
- **Лимит размера стал суммарным на запрос.** `MAX_FILE_SIZE_MB` переименована в `MAX_REQUEST_SIZE_MB`, и сервер меряет тело целиком по `Content-Length` вместо каждого файла по отдельности — в потолок входят и поля формы, а без заголовка остаётся сумма файлов. Поле `/api/status` теперь `max_request_size_mb`, ключ отказа — `serverError.requestTooLarge` без параметра `file`. `client_max_body_size` в обоих конфигах nginx снижен с 4 до 2 МБ, вровень с настройкой, а интерфейс перед отправкой складывает размеры выбранных файлов. Раньше три файла по мегабайту проходили и проверку интерфейса, и пофайловую проверку сервера, а от прокси получали голый 413 — без объяснения и без номера запроса. Совет «разделите документ на части» в тексте отказа заменён на предложение оставить только страницы с картой регистров: части одного документа в тот же запрос всё равно не помещаются. Вставка картинки из буфера идёт через ту же проверку, раньше она добавляла файл вообще без неё.
- **Интерфейс отсекает набор файлов до отправки.** `/api/status` отдаёт `max_files` и `allowed_extensions` рядом с `max_request_size_mb`, и по ним интерфейс не даёт прикрепить файл не того формата и файл сверх потолка — раньше такой набор уезжал на сервер целиком и отклонялся уже после загрузки. Отбор живёт в сторе, поэтому накрывает все входы — дропзону, диалог выбора, вставку из буфера и перетаскивание на пустой экран, которое раньше складывало файлы вообще без проверок. Список расширений во фронте больше не дублируется, диалог выбора тоже строится из ответа сервера. Сервер проверяет то же самое повторно, для клиентов мимо интерфейса.
- **`.xls` больше не принимается.** Расширение убрано из допустимых на сервере — openpyxl читает только xlsx, и раньше такой файл проходил проверку расширения, чтобы упасть на разборе. Список форматов в интерфейсе приведён к списку на бэкенде, `.xls` и `.bmp` из диалога выбора убраны.

### Удалено
- **`pdf_to_images` и зависимости под него.** Функцию не звал никто, PDF уходит в модель файлом как есть, а рендер страниц шёл мимо всех потолков на изображения. Вместе с ней из проекта ушли `pdf2image` и системный пакет `poppler-utils` в образе.

### Исправлено
- **Зависимость `httpx` объявлена явно, `openai` запинен на `3.3.1`.** Код обращается к httpx напрямую, а в окружение он приезжал транзитивно через openai — в 3.x SDK перешёл на `httpx2`, и установка по `requirements.txt` перестала тянуть httpx, сборка падала на `ModuleNotFoundError`. Своя строка нужна и после пина: openai допускает `httpx>=0.23.0`, а сервису нужен минимум 0.26, где у клиента появился аргумент `proxy`. Пин фиксирует версию, на которой сервис проверяется, вместо диапазона, дающего разный рантайм у одного коммита.
- **Нечитаемая книга Excel объясняется текстом, а не «внутренней ошибкой».** Файл с расширением `.xlsx`, который на деле не книга (переименованный docx, обычный zip), openpyxl отвергает не только `BadZipFile`, но и `KeyError` с `OSError` — эти два случая доходили до общего обработчика, пользователь получал «Внутренняя ошибка при анализе документа», а в лог уезжал трейсбек на штатно кривой вход.
- **Перевод не выдаёт свой сбой за неразобранный ответ модели.** Когда провайдер трижды отвергал параметры запроса, цикл правки заканчивался без ответа, и пользователю говорили «ответ модели не удалось разобрать», хотя модель не отвечала вовсе, а дежурный не получал алерта. Теперь такой случай приходит категорией сбоя провайдера.
- **Слишком большая картинка больше не называется битой.** Встроенная бомбозащита Pillow срабатывала раньше нашей проверки на 25 Мпикс и отдавала общий отказ про повреждённый файл вместо текста с разрешением картинки.
- **Нечитаемый ответ модели больше не выдаётся за отказ провайдера.** В `/api/translate` и `/api/models` под классификацией сбоя стоял весь разбор ответа, поэтому проза вместо JSON или HTML от прокси приезжали пользователю как «обращение к провайдеру LLM не удалось» — диагностика уходила к провайдеру, который в этот момент ответил штатно. Свой сбой разбора теперь называется своим ключом, а алерт нотификатора на него не отправляется.
- **Неразбираемый адрес LLM отвечал пятисоткой.** `http://[evil/v1` и подобное `urlsplit` читает как незакрытый IPv6-литерал и бросает `ValueError`, который доходил до глобального обработчика — пользователь получал «Внутренняя ошибка сервера», а в лог уезжал трейсбек. Теперь это обычный отказ проверки адреса с ключом локализации.
- **Лог отката температуры говорил неправду.** Когда модель отвергает присланную температуру, запрос повторяется без неё, но в логе стояло захардкоженное `temperature=0.1` (в переводе — вообще без модели и значения), и про повторный запрос не было сказано. Теперь печатается фактическое значение, модель и то, что запрос уходит второй раз.
- **Пятисотка не несла номер запроса и не попадала в статистику.** Необработанное исключение уходит в `ServerErrorMiddleware`, а он стоит снаружи нашего middleware, поэтому до заголовка `X-Request-Id` и до записи «→ 500» в логе управление не доходило — падение нельзя было найти в логе по номеру из ответа.

## [0.7.5] - 2026-08-10

### Добавлено
- **Проверка пользовательского адреса LLM** на `/api/analyze`, `/api/fix-registers`, `/api/translate` и `/api/models`. Адрес приходит в запросе без авторизации и становится `base_url` клиента, то есть сервер идёт по нему сам — теперь разрешены только `http` и `https`, а имя хоста не должно разрешаться в loopback, RFC1918, link-local, CGNAT (`100.64.0.0/10`), site-local IPv6 (`fec0::/10`), 6to4 (`2002::/16`) и прочие служебные диапазоны. Отказ приходит с ключом локализации, переводы во всех четырёх локалях.
- **Настройка `LLM_ALLOW_PRIVATE_URLS`** (по умолчанию `false`) — разрешает свой адрес LLM во внутренней сети для развёртывания, где LLM стоит рядом. Проверка схемы при этом остаётся, а имя не резолвится вовсе: внутреннее имя снаружи всё равно не разрешается.
- **Отказы проверки адреса видны в логе** — разрешение имени во внутреннюю сеть и молчащий резолвер пишут WARNING с именем хоста, несуществующее имя INFO. Без этого проверка срабатывала бы молча, и отличить опечатку пользователя от целенаправленных попыток было бы нечем.
- **Тесты**: `test_llm_url_guard.py`, `test_llm_http_client_pool.py`, общий `tests/conftest.py`.

### Изменено
- **Редиректы для запросов к LLM запрещены.** `openai-python` поднимает свой httpx-клиент с `follow_redirects=True`, поэтому публичный адрес, отвечающий `302`, увёл бы запрос во внутреннюю сеть в обход проверки. Провайдер, который отвечает редиректом вместо ответа, перестаёт работать — обычные OpenAI-совместимые API редиректов не используют.
- **Общий httpx-клиент процесса** вместо клиента на запрос. Соединения к провайдеру переиспользуются, а не открываются заново с TLS-handshake на каждое обращение, и закрываются явно при остановке сервиса, а не когда до них дойдёт сборщик мусора.
- **Клиентов ровно два, серверный и пользовательский.** У пользовательского трафика свой пул соединений, поэтому чужой медленный хост не занимает соединения серверного, cookie чужих ответов не копятся, а прокси оператора ему не достаётся.
- **Потолок 5 секунд на разрешение имени.** Молчащий NS в присланном адресе иначе занимает резолвер процесса, а `/api/models` и `/api/translate` не троттлятся.
- **dev-compose читает `.env` целиком** (`env_file`), как прод. Поимённый список переменных пришлось бы дописывать под каждую новую настройку, иначе способ «задать в `.env`» в dev молча не работает.
- **Прокси dev-сервера Vite смотрит на порт 9000** — бэкенд слушает его, а не 8000, поэтому `npm run dev` вне докера получал ошибку на каждый запрос к API.

## [0.7.4] - 2026-08-09

### Изменено
- **Настройки LLM приходят в теле запроса** — у `/api/fix-registers` шесть полей были query-параметрами, поэтому ключ пользователя попадал в строку запроса, то есть в access-логи nginx и uvicorn. Поля собраны в модель `LlmOverrides` и приезжают телом, набор у исправления регистров и перевода теперь один и тот же.
- **Приоритет параметров LLM разрешается в одном месте** — `resolve_llm_target` в `backend/llm_service.py`. Правило было скопировано в четыре маршрута с расхождениями, отсюда пункты в «Исправлено». Изоляция доступа не менялась: со своим адресом серверный ключ не подставляется, прокси оператора для чужого адреса не используется.
- **Сентинел `-1` для температуры убран** — «клиент не прислал» теперь везде `None`, а ноль значим и доезжает до провайдера.
- **Присланный таймаут на серверном ключе можно только опустить** — поднять выше `LLM_TIMEOUT` нельзя, уменьшение пишется в лог предупреждением. Длинный запрос держит воркер, а исправление регистров и перевод не троттлятся. На своём адресе значение применяется как есть, там чужой провайдер. `LLM_TIMEOUT=0` по-прежнему значит «без потолка».
- **`docs/api.md`** — добавлены таблица настроек LLM и строка `/api/fix-registers`, исправлены имена параметров `/api/analyze`: в доке стояли `custom_llm_url`, `custom_llm_key`, `custom_llm_model`, `custom_system_prompt`, а код всегда принимал `llm_api_url`, `llm_api_key`, `llm_model`, `system_prompt`.

### Исправлено
- **Кнопка «Исправить через AI» ходила на серверный LLM** — интерфейс не передавал настройки пользователя в `/api/fix-registers`. На своём LLM исправление уходило чужим ключом, а без серверного LLM отвечало «LLM не настроен», хотя анализ и перевод в это время работали на ключ пользователя.
- **Присланные модель, таймаут и температура игнорировались на серверном LLM** в исправлении регистров и переводе, хотя окно «Настройки LLM» их предлагает и на анализе они действовали.

## [0.7.3] - 2026-08-07

### Изменено
- **Ошибки остальных маршрутов локализованы** — ключ локализации теперь несут `/api/analyze`, `/api/models`, `/api/translate`, `/api/build`, валидация и глобальный обработчик. Каталог `backend/user_errors.py` вырос с 8 ключей до 25, переводы добавлены во все четыре локали. Русский текст остаётся в `detail` — для curl, интеграций и лога, а также фолбеком, если ключ интерфейсу незнаком.
- **`docs/api.md` приведён к коду** — в описании SSE-события `error` появились `message_key` и `message_params`, рядом сказано про тот же контракт у HTTP-отказов. Уточнено описание rate limiter — бакет ключуется адресом из соединения, поэтому за обратным прокси он общий на всех клиентов, и висит лимитер только на `/api/analyze`.
- **Ошибки внутри SSE-потока тоже переводятся.** Половина отказов анализа уходит событием `error`, а не HTTP-ответом, и до этого интерфейс показывал их русский текст независимо от выбранного языка. Добавлен `sse_user_error`, событие несёт `message_key` и `message_params` рядом с прежним `message`.

### Исправлено
- **Обёртки `/api/analyze` и `/api/fix-registers` не читали тело ответа** и показывали «Ошибка сервера: N» с кодом. Теперь текст отказа доходит до пользователя, а вместе с ним номер запроса — раньше он терялся, хотя бэкенд его отдавал.
- **Обёртки `/api/build`, `/api/build-jinja` и валидации схемы** тоже игнорировали тело, хотя глобальный обработчик уже отдавал им осмысленный отказ.
- **Текст исключения больше не уходит пользователю при неожиданном отказе** — внутренний сбой анализа показывался как «Ошибка анализа: <текст исключения>», сбой исправления регистров как «Ошибка: <текст>». Теперь оба места отдают фразу из каталога, а исключение пишется только в лог.

## [0.7.2] - 2026-08-06

### Исправлено
- **RCE при импорте Jinja-шаблона**: `template_importer` рендерил загруженный `.json.jinja` обычным `jinja2.Environment`, поэтому гаджет вида `cycler.__init__.__globals__.os.popen` выполнял команды ОС ещё до разбора JSON. Рендер переведён на `SandboxedEnvironment` — доступ к внутренним атрибутам отклоняется с понятной ошибкой, легальные шаблоны (циклы, условия, макросы, `list.append`) работают как прежде.
- **ReDoS при импорте JSON-шаблона**: `_strip_json_comments` чистила комментарии регуляркой `^\s*//` с `re.MULTILINE`, а `\s` матчит перевод строки — на входе из пустых строк разбор был квадратичным (100 КБ ≈ 13 с на воркере), причём `.json`-ветка идёт мимо потолка 1 МБ. Класс заменён на `[ \t]*`, разбор линейный.
- **Отказ импорта не выносит наружу текст неожиданного исключения**: общий `except` отдаёт обобщённую фразу, полный текст остаётся в `logger.exception`. Ошибки самого шаблона (незакрытый `{% for %}`, обращение к несуществующей переменной, позиция ошибки JSON) по-прежнему показываются с текстом и номером строки — без них автор шаблона не поймёт, что чинить.

### Изменено
- **Потолок на исходник Jinja `1 МБ`**: крупнее в рендер не уходит (самый большой боевой `.json.jinja` — 124 КБ). На обычный JSON лимита нет — разбор JSON код не исполняет, а в шаблонах `wb-mqtt-serial` есть JSON до 1.8 МБ.
- **Процесс бэкенда в контейнере запускается от непривилегированного пользователя** `app` (раньше root). Файлы остаются во владении root, поэтому процесс не перезапишет собственный код.
- **Зависимости с CVE подняты и жёстко запинены** — `jinja2==3.1.6` (обязателен, закрывает побег из песочницы через фильтр `attr`), `Pillow==12.3.0`, `python-multipart==0.0.32`, `fastapi==0.115.14`. У трёх и раньше был жёсткий пин, но на уязвимой версии, а `jinja2` шёл диапазоном — теперь прибит тоже, чтобы сборка не поехала сама.
- **Ошибки импорта локализованы**: ответ несёт `message_key` + `message_params`, интерфейс рендерит их на своём языке (RU, EN, KK, IT). Русский текст остаётся в `detail` — для curl, интеграций и лога, а также фолбеком, если ключ интерфейсу незнаком. Контракт тот же, что у валидатора регистров; каталог ключей с текстами — `backend/user_errors.py`. Подстрочный матч по английской фразе в `api.ts` вместе с ключом `api.importNotTemplate` убран за ненадобностью.
- **Ресурсные лимиты песочницы отдают понятный отказ.** `range` больше `MAX_RANGE` даёт `OverflowError`, а он не наследник `TemplateError` и уходил бы в общий обработчик немым 422 с трейсбеком в логе.

### Добавлено
- **Тесты**: `TestJinjaSandbox`, `TestJinjaErrorsAreDiagnostic`, `TestJsonComments` и потолок исходника в `test_template_importer.py`, `test_user_errors.py` (каталог, паритет со словарём интерфейса по ключам и плейсхолдерам в каждой из четырёх локалей, ответы эндпоинта), фронтенд `api-error-detail.test.ts`.

## [0.7.1] - 2026-08-02

### Добавлено
- **Авто-исправление после анализа (одна попытка)**: при ошибках валидации прогоняется `детерминированный auto_fix (бесплатно) → один LLM-фикс кривых регистров → повторная валидация`. Документ в фикс не передаётся, поэтому пропущенные регистры не восстанавливаются — остаток ошибок остаётся под кнопкой «Исправить через AI». Шаги видны в логе анализа как этапы `autofix`.
- **Предупреждение при закрытии страницы во время анализа** — незавершённый анализ восстановить нельзя, его состояние нигде не сохраняется. Готовый шаблон в localStorage это не затрагивает.
- **Метрики автофикса** — блок `analysis` в `/api/metrics` (`autofix_runs`, `autofix_cleared`): по их отношению видно, помогает ли автофикс и нужна ли ручная кнопка. Детали прохода пишутся в лог сервера.
- **`frontend/.dockerignore`** — `node_modules`, `dist`, `.vite` и логи npm больше не уезжают в контекст сборки образа.
- **Тесты**: `test_autofix_in_analysis.py`, `test_fix_registers_core.py`, `test_merge_batch_results.py`, `test_queue_manager.py`, `test_queue_wiring.py`, `TestLegacySixDigitAddress` в `test_register_validator.py`, `api-models.test.ts`.

### Изменено
- **`MAX_FILE_SIZE_MB` по умолчанию `1` → `2`**, лимит тела nginx поднят `2m` → `4m` (запас на multipart-оверхед).
- **Ключ дедупа регистров** `(address, reg_type)` → `(address, reg_type, condition)`: condition-gated пары (один адрес и тип, взаимоисключающие condition) больше не схлопываются в одну строку.
- **Очередь переписана на `asyncio.Semaphore`** вместо самодельного учёта слотов на `asyncio.Event`: слот занимает только `QueueTicket.wait()`, а освобождает только выход из `AnalyzeQueue.ticket()`, поэтому освободить незанятый слот стало невозможно, а лимит не превышается при одновременном освобождении и входе. Наблюдаемое поведение то же.
- **Рефакторинг**: ядро исправления регистров вынесено в `_fix_registers_core()` — общий код для кнопки «Исправить через AI» и автофикса. Ожидание ответа LLM с keepalive вынесено в `_await_batch_with_progress()`.

### Исправлено
- **Очереди анализа не подключались вообще**: `main.py` импортировал `server_queue`/`custom_queue` по значению, а `init_queues()` присваивает их внутри `queue_manager` — в `main` они навсегда оставались `None`. Поэтому `QUEUE_SERVER_MAX_CONCURRENT` и `QUEUE_CUSTOM_MAX_CONCURRENT` не действовали, позиция в очереди в UI не показывалась, `/api/queue-status` и `/api/health` отдавали `null`. Теперь обращение идёт через модуль.
- **Адреса в шестизначной legacy-нотации 4X/3X приезжали неконвертированными** (Eastron SDM630MCT и другие даташиты, где 5- и 6-значная запись смешаны в одной таблице): канал получал адрес больше 65535 и на железе не опрашивался, а валидатор этого не замечал. Промпт распространяет конвертацию на шестизначную запись, `auto_fix_register` правит такой адрес детерминированно до парсинга, валидатор считает `address > 65535` ошибкой.
- **Запрос к LLM висел после ухода пользователя**: при обрыве SSE задача жила до `LLM_TIMEOUT` и жгла квоту, хотя доставить результат уже некуда. Теперь обрыв потока отменяет запрос к провайдеру — в анализе, автофиксе и ручной кнопке.
- **Длинный LLM-проход автофикса рвал SSE-соединение**: событий в поток не уходило, а nginx закрывает соединение после 300 секунд молчания upstream, и пользователь терял уже выполненный анализ. Теперь автофикс ждётся через тот же keepalive-цикл, что и основной анализ.
- **`analyze_duration_avg` включал время ожидания в очереди**, а оценка ожидания считается из этой средней. Теперь замер идёт с момента получения слота, а не дошедшие до анализа запросы в гистограмму не попадают — поэтому `analyze_duration_count` меньше `analyze_total`.
- **Кнопка «Загрузить список моделей» не работала в серверном режиме**: фронт слал пустой `multipart/form-data`, FastAPI отвечал `400 There was an error parsing the body`. Теперь без своего URL и ключа тело не отправляется, в кастомном режиме как прежде.
- **Ошибка обращения к LLM API больше не выдаётся за проблему с документом** — при неверном ключе, исчерпанной квоте или недоступности провайдера сообщение предлагало проверить формат документа и уводило диагностику не туда.
- **Битое изображение прекращает анализ понятным сообщением** с именем файла вместо технического текста PIL. Файл проверяется до обращения к LLM, поэтому запрос к провайдеру не уходит.
- **Пустой результат анализа больше не проезжает молча**: если после мержа регистров нет, отдаётся понятная ошибка вместо пустого шаблона.
- **Перечень SSE-стадий в документации не совпадал с кодом**: в `README.md` не было `validating` и `autofix`, в `docs/api.md` — `slow`. Приведено к коду, `slow` описан как замена `analyzing` после мягкого таймаута, а не отдельное звено цепочки.

### Удалено
- **Настройка `PDF_BATCH_SIZE`** — осталась от вырезанного дробления PDF, в коде не читалась, но была документирована в пяти местах. Строку из прод-`.env` можно не убирать: `env_file` в compose передаёт её переменной окружения, а неизвестные переменные окружения pydantic-settings игнорирует.
- **Модуль `backend/mock_data.py`** и упоминания mock-режима в документации: `use_mock()` не вызывался ниоткуда, а при пустом `LLM_API_URL` анализ отвечает «LLM не настроен», то есть описанного в README режима работы без LLM не существовало.
- **Эндпоинт `POST /api/cancel-analyze`** и путь отмены по `request_id`: кнопка «Отменить» и раньше рвала SSE-соединение через `AbortController`, а обрыв стрима сам снимает ожидание в очереди.

## [0.7.0] - 2026-07-16

### Добавлено
- **Тест** `test_llm_content.py` — контракт сборки LLM-контента (`assemble_llm_content`).

### Изменено
- **Модель по умолчанию `gpt-4o` → `gpt-5.6-luna`**: gpt-4o теряет регистры на image-based PDF, все gpt-5.x дают 100%; luna выбрана по балансу цена/качество (~$0.02/анализ).
- **`LLM_TEMPERATURE` по умолчанию → пусто**: gpt-5.x принимают только `temperature=1`. Для gpt-4o/локальных LLM детерминизм возвращается установкой `LLM_TEMPERATURE=0`.
- **Рефакторинг**: сборка vision-контента вынесена в `assemble_llm_content()`. Поведение не изменилось.
- **Дефолт модели сведён в один источник** — `config.py` (в `docker-compose.yml` убран дублирующий хардкод).

### Исправлено
- **Текстовый prompt-regression** был нерабочим с 0.6.0: `run.py` распаковывал 2 значения вместо 3 (`too many values to unpack`).

## [0.6.4] - 2026-07-13

### Добавлено
- **`register_validator`: проверка имени канала на запрещённые символы** — символы `$ # + / \ " '` (MQTT-разделители/wildcard и кавычки) ломают имя топика, и схема драйвера отклоняет такой шаблон при экспорте. Раньше это ловилось только на этапе скачивания (`schema_validator`), теперь — в реальном времени в редакторе.

### Изменено
- **Проверка по JSON-схеме драйвера при скачивании включена по умолчанию** — чекбокс в модалке скачивания теперь активен изначально и проверка запускается автоматически при открытии. Это дешёвая (локальная, без LLM) финальная проверка собранного шаблона: ловит ошибки сборки/структуры, которые не видны в пофайловой валидации регистров.

### Удалено
- **`register_validator`: убрана эвристика `probablySwitch`** («Enable/Disable/On-Off» в имени → возможно, должен быть switch). Это была единственная догадка по тексту имени, а не факт из схемы/драйвера, и она давала ложные срабатывания на легитимных value-каналах.

### Исправлено
- **Кнопка «Исправить через AI» больше не виснет на крупных шаблонах.** Раньше в LLM уходил весь шаблон с ожиданием полного исправленного набора в ответ — на устройствах с сотнями каналов (напр. WB-MR6C, 555) запрос длился минутами, вывод обрезался по лимиту токенов (`finish_reason=length`) → падал парсинг → кнопка «висла». Теперь в LLM уходят **только регистры с ошибками** (ERROR), а исправления вмёрдживаются обратно в полный шаблон строго по позициям (через временный тег `__fix_<i>`, т.к. `id` в шаблонах не уникальны — у condition-gated пар совпадают). Пустой набор ошибок → возврат шаблона без вызова LLM.
- **Подсказки fix-потока приведены к JSON-схеме драйвера.** Промпт «Исправить через AI» и `format_validation_errors` больше не хардкодят устаревший список `reg_type` (6 значений) — берут его из того же schema-derived перечня, что и валидатор. Иначе LLM могла «чинить» валидный `press_counter` обратно в неверное значение. Промпт также велит не трогать поля, которых нет в ошибках.
- **`register_validator`: убраны ложные ошибки/предупреждения, расходившиеся с драйвером wb-mqtt-serial** (сверено по исходникам драйвера). На реальном шаблоне WB-MR6C это давало 28 ложных ошибок + 35 ложных предупреждений — стало 0/0:
  - **reg_type/format/word_order/byte_order/channel_type теперь берутся из JSON-схемы драйвера** (той же, что у `schema_validator`), а не из хардкод-списка. Раньше `VALID_REG_TYPES` содержал 7 значений, тогда как драйвер знает ~45 (набор зависит от протокола устройства) — валидные типы `press_counter`, `relay`, `power`, `freq` и др. ложно помечались как ошибка; аналогично тип канала `w1-id` отсутствовал в хардкод-списке. Теперь списки не дрейфуют от драйвера.
  - **«Дубликат адреса» не срабатывает на condition-gated парах** (одна address, взаимоисключающие `condition`) — это штатный паттерн, драйвер оставляет один канал.
  - **Удалены правила без основания в драйвере**: «switch не использует enum» (драйвер публикует enum независимо от типа), «coil/discrete допускают только switch-подобные типы» (драйвер тип канала не ограничивает), «wo-switch должен быть write-only» (драйвер сам принудительно делает write-only), «многорегистровый формат требует word_order» (у драйвера дефолт big-endian).
  - **`string_data_size` не требуется**, если ширина строки задана через адрес `"N:offset:width"`.

## [0.6.3] - 2026-06-29

### Исправлено
- **Потеря флага readonly при round-trip**: при загрузке существующего JSON-шаблона атрибут `readonly` (без подчёркивания) у каналов и параметров не переносился в модель редактора — экспорт смотрит на `reg.readonly`/`reg.read_only`, а импортёр выставлял только `access`. В итоге при «скачал → загрузил обратно» отметки readonly пропадали. Добавлено `readonly` в roundtrip-поля импортёра.

## [0.6.2] - 2026-05-07

### Добавлено
- **Telegram-топики**: новая переменная `TELEGRAM_MESSAGE_THREAD_ID` — алерты можно отправлять в конкретный топик супергруппы (forum supergroup), а не только в общий чат. Если не задана — поведение прежнее (отправка в `TELEGRAM_CHAT_ID`).
- **Telegram-уведомления о сбоях LLM API**: классификация ошибок OpenAI SDK по 10 категориям (`quota_exceeded`, `auth`, `permission`, `not_found`, `bad_request`, `rate_limit`, `timeout`, `connection`, `server_error`, `unknown`) и серьёзности (CRITICAL/WARNING)
- **Антиспам**: CRITICAL — cooldown по категории (15 мин), WARNING — порог событий в окне времени (5 событий за 5 мин)
- **Билинговые алерты**: распознаются маркеры `insufficient_quota`, `billing_hard_limit_reached`, `billing_not_active`, `account_deactivated` (двойная проверка по `error.code` и тексту — для совместимых провайдеров) — алерт уходит немедленно
- **API**: блок `llm_errors_by_category` в `/api/metrics` — счётчики ошибок LLM API по категориям (только для серверного LLM)
- **Изоляция**: ошибки пользовательских (custom) LLM не репортятся — чужой ключ не наша зона ответственности
- **Настройки**: переменные `TELEGRAM_*` в `.env` (`TELEGRAM_NOTIFY_ENABLED`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, `TELEGRAM_PROXY` и др. — см. `docs/config.md`)
- **Тесты**: 42 теста (20 — классификатор, 19 — notifier, 3 — интеграция в `analyze_document`)

## [0.6.1] - 2026-04-09

### Добавлено
- **Валидация по схеме драйвера**: опциональная проверка собранного шаблона по JSON-схеме `wb-mqtt-serial-device-template.schema.json` при скачивании
- **UI**: модалка подтверждения при скачивании со статусом валидации и чекбоксом «Проверить по схеме драйвера»
- **API**: эндпоинт `POST /api/validate-schema` — валидация шаблона по JSON-схеме драйвера
- Фильтрация ложных ошибок схемы (oneOf-варианты протоколов, bitwise-адреса)
- Человекочитаемые сообщения об ошибках схемы с i18n (4 локали)

## [0.6.0] - 2026-04-09

### Добавлено
- **Валидация регистров**: проверка format, reg_type, channel_type, address, enum по JSON-схеме драйвера wb-mqtt-serial (`wb-mqtt-serial-confed-common.schema.json`)
- **Авто-исправление**: синонимы LLM-ответов автоматически нормализуются (`uint16`→`u16`, `holding_register`→`holding`, `rw`→`readwrite` и т.д.)
- **LLM retry по валидации**: при >30% ошибок в ответе LLM — автоматический повторный запрос с описанием ошибок
- **UI**: красные/жёлтые бейджи ошибок валидации в строках таблицы регистров (тултип с описанием)
- **UI**: подсветка полей с ошибками в панели деталей (name, word_order, byte_order, string_data_size)
- **UI**: счётчик ошибок/предупреждений в тулбаре
- **UI**: кнопка «Исправить через AI» — отправка регистров с ошибками в LLM для автоматического исправления
- **UI**: модалка-предупреждение при скачивании шаблона с ошибками валидации («Скачать всё равно?»)
- **SSE**: новая стадия `validating` — отображается при проверке регистров после анализа
- **API**: эндпоинт `POST /api/validate` — валидация регистров, возвращает ошибки с i18n-ключами
- **API**: эндпоинт `POST /api/fix-registers` — SSE-исправление регистров через LLM
- **Форматы**: добавлены s24, u24, bcd8, bcd16, bcd24, bcd32, char8, string8 (из схемы драйвера)
- **Тесты**: 66 тестов бэкенд-валидатора, 11 тестов фронтенд-утилит валидации

### Изменено
- **Константы**: `FORMATS` расширен с 11 до 19 значений, добавлен `direct` в `REG_TYPES`
- **LLM pipeline**: `_parse_registers()` теперь применяет авто-исправление перед Pydantic-парсингом

## [0.5.9] - 2026-03-04

### Добавлено
- **CI**: проверка версии в CHANGELOG.md — первая секция должна быть версионной `[X.Y.Z]`, не `[Unreleased]`
- **CI**: автоматическое создание git-тега из версии в CHANGELOG при merge в main

### Исправлено
- **CHANGELOG**: восстановлен формат Keep a Changelog — секция `[Unreleased]`, compare-ссылки для всех версий

## [0.5.8] - 2026-03-04

### Исправлено
- **UI**: запрет недопустимых channel_type для параметров — параметры допускают только `value`, тип `switch` и другие доступны только для каналов
- **UI**: при переключении регистра в параметр channel_type автоматически сбрасывается на `value`
- **UI**: скрыты поля on_value/off_value для параметров (не применимы без switch)
- **LLM-промпт**: явная инструкция — параметры не используют `switch`, для вкл/выкл использовать `enum`

## [0.5.7] - 2026-02-27

### Добавлено
- **UI**: футер со ссылкой на исходный код на GitHub
- **UI**: бейдж версии — кликабельная ссылка на CHANGELOG в репозитории
- **UI**: tooltip «by AI» — код написан и поддерживается AI
- **UI**: валидация condition — красный бейдж ⚠ если condition ссылается на канал вместо параметра

### Изменено
- **UI**: dropdown «Скачать» — подсказки к форматам: JSON — готовый шаблон, Jinja — исходник для компиляции
- **UI**: tooltip «Вкл» уточнён — опрос регистра по умолчанию (в шаблон попадает в любом случае)
- **UI**: tooltip «Параметр» уточнён — параметр vs канал
- **UI**: колонка «Канал» → «Контрол», подсказка — представление регистра в веб-интерфейсе

### Исправлено
- **LLM-промпт**: усилен запрет на condition-ссылки на каналы — condition может зависеть только от параметров
- **Тесты**: 10 тестов на валидацию condition (ссылка на канал vs параметр, операторы, совпадение по id/name/original_channel_id)

## [0.5.6] - 2026-02-26

### Добавлено
- **Тесты**: изоляция серверного API-ключа — 10 тестов на утечку ключа, промпта и прокси при пользовательском LLM (analyze, models, translate)

### Изменено
- **Рефакторинг**: логика маршрутизации LLM-ключей вынесена в `resolve_llm_credentials()` — единая точка изоляции для analyze, models, translate

## [0.5.5] - 2026-02-26

### Добавлено
- **Тесты**: prompt regression тесты — 5 ручных кейсов для проверки LLM-промпта (адреса FC, legacy 5-digit, plain, bitwise, coil/discrete→switch). Запуск: `docker compose exec backend python tests/prompt_regression/run.py`

## [0.5.4] - 2026-02-26

### Исправлено
- **UX**: при импорте невалидного JSON (не wb-mqtt-serial) ошибка теперь отображается заметным блоком внутри hero-блока, а не мелким баннером в шапке (#16)
- **UX**: hero-блок показывает loading-спиннер во время импорта шаблона — `importing` из store теперь используется в UI
- **LLM-промпт**: bitwise-формат адреса (`reg:bit:width`) теперь явно ограничен только holding-регистрами — для coil, discrete и input не применяется
- **LLM-промпт**: исправлена конвертация адресов — убраны legacy-форматы 0xxxx/1xxxx (путали LLM), добавлено явное правило для таблиц с колонкой Function Code (FC 01/02/03/04 → тип из FC, адрес как есть)

## [0.5.3] - 2026-02-21

### Исправлено
- **UI**: заголовки групп в таблице регистров отображаются на русском при RU-интерфейсе (из `group.translations.ru.title`)
- **UI**: кнопка i18n и блок переводов в GroupManager скрыты для EN/KZ/IT — аналогично остальным компонентам

## [0.5.2] - 2026-02-21

### Добавлено
- Тесты: покрытие i18n (полнота переводов, интерполяция, fallback, согласованность плейсхолдеров)
- Тесты: валидация импорта (parameters_only, device_id_without_device_type)
- **LLM**: переводы названий групп (`group_title_translations`) — LLM генерирует переводы заголовков групп, фронтенд использует их при автогенерации групп

### Исправлено
- **LLM-промпт**: при отсутствии языков переводов (EN/KZ/IT локаль) LLM больше не генерирует ненужные русские переводы — фронтенд не отправляет `translation_languages`, бэкенд стрипает лишние переводы из ответа LLM
- **Баг**: клик по drop zone и кнопке «Импорт шаблона» не открывал file dialog — заменено на нативный `<label>`

## [0.5.1] - 2026-02-21

### Изменено
- **UX**: тулбар скрыт при пустой таблице — hero-блок полностью заменяет навигацию
- **UX**: кнопки «Анализ» и «Импорт шаблона» убраны из тулбара — доступны через hero-блок при пустом состоянии
- **UX**: метка «by AI» в заголовке приложения

## [0.5.0] - 2026-02-21

### Добавлено
- **i18n**: мультиязычный интерфейс — 4 языка (RU, EN, KZ, IT) с автодетектом из браузера
- **i18n**: переключатель языка в шапке — dropdown-меню с выбором из 4 языков
- **i18n**: лёгкая система переводов без внешних зависимостей (`useT()` хук + `getT()` для non-React)
- **UX**: hero-блок при пустой таблице — 3 карточки с drag-n-drop: AI-анализ, импорт шаблона, CSV/ручной ввод
- **UX**: hero drop zone с роутингом по расширению файла (.json→импорт шаблона, .csv→CSV, остальное→AI)
- **UX**: кнопка «Анализ» с иконкой sparkle в тулбаре — прямой доступ к AI-анализу
- **UX**: dropdown «AI» — автоперевод, нормализация, настройки LLM
- Валидация JSON при импорте шаблона — отклоняет файлы без channels/parameters/device_type
- Конфигурация `LOCALE_TRANSLATION_LANGUAGES` — управление доступностью переводов по локалям

### Изменено
- Тулбар реструктурирован: файловые операции перенесены из шапки в тулбар таблицы
- Шапка компактная: заголовок + версия + поля устройства + язык — в одну строку
- Все UI-строки (~250 ключей) переведены на 4 языка
- FORMAT_OPTIONS: `description` → `descriptionKey` для совместимости с i18n
- Нормализация → EN работает для любого нелатинского текста (не только кириллица)
- Переводы (языки, автоперевод) скрыты для EN/KZ/IT — контроллер пока поддерживает только RU+EN
- Язык интерфейса по умолчанию — EN

## [0.4.0] - 2026-02-19

### Добавлено
- Автоматическое версионирование: backend парсит версию из CHANGELOG.md при старте
- Отображение версии приложения в шапке UI (бейдж `vX.Y.Z`)
- CI: проверка обновления CHANGELOG.md в pull request (мягкое предупреждение)
- `.dockerignore` для оптимизации контекста сборки

### Изменено
- CHANGELOG.md — единственный источник истины для версии приложения
- Docker: build context backend'а перенесён на корень проекта для доступа к CHANGELOG.md

### Исправлено
- **Безопасность**: полная изоляция серверных настроек LLM (ключ, прокси) — при пользовательском URL серверные настройки не используются
- Пользовательские настройки LLM (temperature, timeout, legacy_max_tokens) теперь передаются в `/api/translate`
- Фикс `crypto.randomUUID()` в non-secure context (HTTP) (#11)
- Версия в коде (`main.py`) синхронизирована с CHANGELOG.md (была захардкожена `0.2.0`)

## [0.3.0] - 2026-02-18

### Добавлено
- Фронтенд unit-тесты для Zustand store (75 тестов, Vitest)
- CI: шаг `npm test` во frontend job

## [0.2.0] - 2026-02-18

### Добавлено
- CI/CD: GitHub Actions (lint, typecheck, тесты бэкенда с покрытием ≥70%)
- Docker prod-конфигурация (`docker-compose.prod.yml`, HEALTHCHECK)
- Nginx: gzip, security headers, кеш статики
- Ruff, mypy, pytest-cov в зависимостях бэкенда
- README: возможности, настройки, скриншоты

### Исправлено
- Ошибки линтера фронтенда: `let`→`const`, неиспользуемые переменные (#8)

## [0.1.0] - 2026-02-18

### Добавлено
- Начальный релиз: загрузка PDF/Excel/изображений, анализ через LLM, визуальный редактор регистров
- Детерминированная сборка JSON-шаблона для wb-mqtt-serial
- Jinja-экспорт с детекцией паттернов (числовые, строковые, вариантные циклы)
- Импорт существующих `.json` / `.json.jinja` шаблонов
- Система переводов на N языков с автопереводом через LLM
- Нормализация кириллицы → EN с сохранением русского в translations
- DnD перемещение регистров между группами
- CSV импорт/экспорт регистров
- SSE-стриминг прогресса анализа с очередями и rate limiting
- Подсветка condition-связей между каналами и параметрами

### Исправлено
- SSE-парсер, Excel→текст конвертация, дефолты LLM (#3)
- Адреса Modbus, клик-навигация по конфликтам переводов (#4)
- Рендер параметров, удалён alarm channel_type (#5)
- Уточнение wo-switch vs switch в промпте LLM (#6)

[0.7.10]: https://github.com/wirenboard/wb-template-generator/compare/v0.7.9...v0.7.10
[0.7.9]: https://github.com/wirenboard/wb-template-generator/compare/v0.7.8...v0.7.9
[0.7.8]: https://github.com/wirenboard/wb-template-generator/compare/v0.7.7...v0.7.8
[0.7.7]: https://github.com/wirenboard/wb-template-generator/compare/v0.7.6...v0.7.7
[0.7.6]: https://github.com/wirenboard/wb-template-generator/compare/v0.7.5...v0.7.6
[0.7.5]: https://github.com/wirenboard/wb-template-generator/compare/v0.7.4...v0.7.5
[0.7.4]: https://github.com/wirenboard/wb-template-generator/compare/v0.7.3...v0.7.4
[0.7.3]: https://github.com/wirenboard/wb-template-generator/compare/v0.7.2...v0.7.3
[0.7.2]: https://github.com/wirenboard/wb-template-generator/compare/v0.7.1...v0.7.2
[0.7.1]: https://github.com/wirenboard/wb-template-generator/compare/v0.7.0...v0.7.1
[0.7.0]: https://github.com/wirenboard/wb-template-generator/compare/v0.6.4...v0.7.0
[0.6.4]: https://github.com/wirenboard/wb-template-generator/compare/v0.6.3...v0.6.4
[0.6.3]: https://github.com/wirenboard/wb-template-generator/compare/v0.6.2...v0.6.3
[0.6.2]: https://github.com/wirenboard/wb-template-generator/compare/v0.6.1...v0.6.2
[0.6.1]: https://github.com/wirenboard/wb-template-generator/compare/v0.6.0...v0.6.1
[0.6.0]: https://github.com/wirenboard/wb-template-generator/compare/v0.5.9...v0.6.0
[0.5.9]: https://github.com/wirenboard/wb-template-generator/compare/v0.5.8...v0.5.9
[0.5.8]: https://github.com/wirenboard/wb-template-generator/compare/v0.5.7...v0.5.8
[0.5.7]: https://github.com/wirenboard/wb-template-generator/compare/v0.5.6...v0.5.7
[0.5.6]: https://github.com/wirenboard/wb-template-generator/compare/v0.5.5...v0.5.6
[0.5.5]: https://github.com/wirenboard/wb-template-generator/compare/v0.5.4...v0.5.5
[0.5.4]: https://github.com/wirenboard/wb-template-generator/compare/v0.5.3...v0.5.4
[0.5.3]: https://github.com/wirenboard/wb-template-generator/compare/v0.5.2...v0.5.3
[0.5.2]: https://github.com/wirenboard/wb-template-generator/compare/v0.5.1...v0.5.2
[0.5.1]: https://github.com/wirenboard/wb-template-generator/compare/v0.5.0...v0.5.1
[0.5.0]: https://github.com/wirenboard/wb-template-generator/compare/v0.4.0...v0.5.0
[0.4.0]: https://github.com/wirenboard/wb-template-generator/compare/v0.3.0...v0.4.0
[0.3.0]: https://github.com/wirenboard/wb-template-generator/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/wirenboard/wb-template-generator/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/wirenboard/wb-template-generator/releases/tag/v0.1.0
