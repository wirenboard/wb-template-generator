# ci/

Свои скрипты — только там, где готового GitHub Action нет. Остальное делают действия:
сборка и пуш образов — `docker/build-push-action`, доставка и команды на сервере —
`appleboy/scp-action` + `appleboy/ssh-action` (через composite-действие
`.github/actions/deploy`), журнал выкатов — `bobheadxi/deployments`, проверка CHANGELOG
в PR — `dangoslen/changelog-enforcer`.

| Скрипт | Зачем свой |
|---|---|
| `shell/smoke.sh` | сверяет `revision` из `/api/status` с выкаченным SHA — контракт нашего сервиса |
| `shell/guard_staleness.sh` | сравнение с `origin/main` перед выкатом |

## last-good и журнал

Источник правды о выкатах — журнал GitHub Deployments: последний `success` = то, что
в проде, на него же опирается откат. Файл `last-good-sha` (+ `.prev`) на сервере —
break-glass-зеркало на случай недоступного GitHub, пишется после успешного smoke.

## Секреты и переменные (среда `production`)

**Секреты:** `DEPLOY_SSH_KEY`, `DEPLOY_HOST`, `DEPLOY_USER`.
**Переменные:** `DEPLOY_DIR`, `PROD_URL`, `DEPLOY_HOST_FINGERPRINT`.

Репозиторий публичный, логи прогонов видны всем — отсюда деление. В переменных лежит то,
что и так публично (адрес сайта, отпечаток сервера) или не даёт доступа (путь на сервере);
как секреты они лишь мешали бы читать логи: GitHub заменяет значение секрета на `***` по
всему выводу. По той же причине держи в уме: `DEPLOY_USER` — секрет, поэтому слово-значение
будет вымарано и в посторонних строках лога.

На сервере в `DEPLOY_DIR` рядом с compose лежит `.env` с ключами LLM — он там уже есть,
старый прод без него не поднялся бы (`env_file: .env`).

Адрес публикации задаётся одной переменной `WEB_PUBLISH`, по умолчанию `127.0.0.1:8080` —
именно то, что ждёт nginx хоста, так что в `.env` её можно не задавать. На `0.0.0.0` менять
нельзя: приложение окажется в интернете мимо TLS. Лежащая в `.env` `WEB_PORT` относится к
прежнему `docker-compose.prod.yml` и новым файлом не используется.

Скрипты проверяются `make lint-shell` (shellcheck + `bash -n`) в составе `make lint`.
