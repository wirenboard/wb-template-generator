# План разработки — бэклог

## Компонентные тесты фронтенда

Сейчас 75 store-тестов (Vitest). Нет компонентных тестов (React Testing Library). Приоритетные кандидаты:
- `RegisterTable` — hero-блок, тулбар скрытие, condition-подсветка
- `App` — переключатель языка, импорт файла

## Расширение i18n

- Добавить языки (DE, FR, ES, ZH, ...)
- Включить переводы регистров для KZ/IT (когда контроллер поддержит)
- Плюрализация (сейчас нет — `{count} рег.` без склонения)

## Улучшения AI-анализа

- Потоковое отображение частичных результатов (progressive rendering)
- Повторный анализ с корректировками (feedback loop)
- Сравнение результатов нескольких моделей

## UX

- Undo/redo для операций редактирования
- Bulk-операции: массовое изменение group, reg_type, format
- Keyboard shortcuts: сделаны Ctrl+Z, Insert, Delete, обход таблицы по Tab; остаётся Ctrl+S
- Поиск/фильтрация в таблице регистров
